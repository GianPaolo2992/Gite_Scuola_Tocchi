package com.example.demo.repository;

import com.example.demo.entity.Gita;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GitaRepository extends JpaRepository<Gita, Integer> {
}
